VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google Client Secret (handled by Rust backend, NOT exposed to frontend)
# For local dev: set here and it will be read by build.rs
# For CI: set as GitHub Actions secret (GOOGLE_CLIENT_SECRET)
GOOGLE_CLIENT_SECRET=your-client-secret
